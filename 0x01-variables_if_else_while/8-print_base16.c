#include <stdio.h>

/**
 * main - prints all the numbers of base 16 in lowercase
 *
 * Return: 0
 */
int main(void)
{
	int k = 48, d = 97;

	while
	(k <= 57)
	{
	putchar(k);
	k++;
	}
	while
	(d <= 102)
	{
	putchar(d);
	d++;
	}
	putchar('\n');
	return (0);
}
